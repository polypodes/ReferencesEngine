<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />

    <title>{% block title %}Réfactor 2000{% endblock %}</title>

    {% block stylesheets %}
      <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}" type="text/css" />
      <link rel="stylesheet" href="{{ asset('js/fancybox/jquery.fancybox.css') }}" type="text/css" media="screen" />
      <link rel="stylesheet" href="{{ asset('css/mycss.css') }}" type="text/css" />
    {% endblock %}
  </head>

  <body>
    <div class="navbar navbar-default navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <a class="navbar-brand">Réfactor 2000</a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li {% block project_active %}{% endblock %} ><a href="{{ path('refactor_projects')}}">{{ 'Projects'|trans }}</a></li>
            <li {% block book_active %}{% endblock %} ><a href="{{ path('refactor_books')}}">{{ 'Books'|trans }}</a></li>
            <li {% block media_active %}{% endblock %} ><a href="{{ path('refactor_medias')}}">{{ 'Media'|trans }}</a></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{ 'Language'|trans }}<b class="caret"></b></a>
              <ul class="dropdown-menu">
                {% for locale in ['en', 'fr'] %}
                    <li><a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale' : locale})) }}">
                      {% if locale == "fr"%}
                      Français
                      {% endif %}
                      {% if locale == "en"%}
                      English
                      {% endif %}
                    </a></li>
                {% endfor %}
              </ul>
            </li>
          </ul>
        </div>
      </div>
    </div>
    <div class="container">
      {% block body %}
      {% endblock %}
    </div>
    <div id ='translate' data-translate-suppress="{{ 'Suppress'|trans }}" data-translate-tag-add="{{ 'tag.Add'|trans }}" data-translate-media-add="{{ 'media.Add'|trans }}" data-translate-render-add="{{ 'render.Add'|trans }}" data-translate-project-choose-="{{ 'project.Choose'|trans }}" data-translate-yes="{{ 'Yes'|trans }}" data-translate-project-notdeleted="{{ 'project.NotDeleted'|trans }}" data-translate-book-add="{{ 'book.Add'|trans }}" data-translate-book-delete="book.Delete" data-translate-noresult="NoResult" data-translate-cancel="{{ 'project.NotDeleted'|trans }}" </div>
    {% block javascripts %}
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.7/jquery.min.js"></script>
    <script type="text/javascript" src="{{ asset('js/bootstrap.min.js') }}"></script>
    <script type="text/javascript" src="{{ asset('js/jquery-2.1.0.min.js') }}"></script>
    <script type="text/javascript" src="{{ asset('js/noty-2.2.2/js/noty/packaged/jquery.noty.packaged.min.js') }}"></script>
    <script type="text/javascript" src="{{ asset('js/fancybox/jquery.fancybox.pack.js') }}"></script>
    <script type="text/javascript" src="{{ asset('js/dropzone.js') }}"></script>
    <script type="text/javascript" src="{{ asset('js/jquery-ui-1.9.2.custom.js') }}"></script>
    <script type="text/javascript" src="{{ asset('js/myjs.js') }}"></script>
    {% endblock %}
  </body>
</html>